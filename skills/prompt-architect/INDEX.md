# 提示词架构师 2.0 · 章节索引

> 本文件由 `scripts/gen-skill-index.mjs` 从源文件**机械生成**（行号即 `read` 的 offset），
> 并由 `tests/unit/skills-contract.test.mjs` 核对——改了源文却不重跑生成器，测试会红。
> 为什么要它：源文几千行，整份读进来要十几万 token。按需读，先查这里。

- 源文件：`src_full.txt`（65 行 / 1270 非空白字符）
- 一级小节：**6** 个（这份源文没有"第X章"编号）

> 原文是 `提示词架构师2.0版本.docx`（Word 二进制，**read 工具读不了**）。
> `src_full.txt` 是从它抽出的可读文本（`node scripts/extract-docx.mjs`）——只有 65 行，
> 需要精确照抄输出格式时**整份读**即可，不用挑章。

## 章节 → 行号

| 节 | 标题 | 行号 | 这一节在讲什么 |
|---|---|---|---|
| 1 | Profile | 2–6 | Version: 2.0 |
| 2 | Goals | 7–11 | 1. 分析用户的原始需求，识别其潜在意图和关键缺失信息。 |
| 3 | Workflow | 12–40 | ◦ 分析用户提供的原始需求、目标受众、语气风格等信息，识别潜在意图和关键缺失信息。 |
| 4 | Rules | 41–55 | • 不提供未经验证的提示词 |
| 5 | Initialization | 56–57 | 现在，请向用户做一个简短的自我介绍，说明你可以如何帮助他们优化提示词，并邀请用户输入他们的原 |
| 6 | Verification Checklist | 58–65 | 1. 提示词包含完整的角色、背景、目标、约束、工作流和输出格式 |

## 源文件已知缺陷（不是我加的，是原始材料就有的）

- 没发现缺章、重号、错位或残片。
